          {/* Profile Sidebar */}
          <div className="space-y-6">
            {/* Profile Completion Card */}
            <Card className="border-2 border-slate-200">
              <CardHeader className="pb-3">
                <CardTitle className="flex items-center gap-2 text-lg">
                  <Target className="w-5 h-5 text-slate-700" />
                  Profile Strength
                </CardTitle>
              </CardHeader>
              <CardContent>
                <div className="space-y-3">
                  <div className="flex items-center justify-between">
                    <span className="text-3xl font-bold text-slate-900">
                      {calculateProfileCompletion(portalData.candidate)}%
                    </span>
                    <Badge variant={calculateProfileCompletion(portalData.candidate) === 100 ? "default" : "secondary"}>
                      {calculateProfileCompletion(portalData.candidate) === 100 ? "Complete" : "In Progress"}
                    </Badge>
                  </div>
                  <Progress value={calculateProfileCompletion(portalData.candidate)} className="h-3" />
                  <p className="text-sm text-muted-foreground">
                    {calculateProfileCompletion(portalData.candidate) === 100
                      ? "Your profile is complete!"
                      : `${100 - calculateProfileCompletion(portalData.candidate)}% away from completion`}
                  </p>
                </div>
              </CardContent>
            </Card>

            {/* Basic Information */}
            <Card>
              <CardHeader>
                <div className="flex items-center justify-between">
                  <CardTitle className="flex items-center gap-2">
                    <User className="w-5 h-5" />
                    Basic Info
                  </CardTitle>
                  <Button
                    variant="outline"
                    size="sm"
                    onClick={() => toast({ title: "Profile editing coming soon!" })}
                  >
                    <Edit className="w-4 h-4" />
                  </Button>
                </div>
              </CardHeader>
              <CardContent className="space-y-4">
                <div>
                  <p className="text-xs text-muted-foreground mb-1">Name</p>
                  <p className="font-semibold text-slate-900">{portalData.candidate.name}</p>
                </div>

                <div>
                  <p className="text-xs text-muted-foreground mb-1">Email</p>
                  <p className="font-medium text-slate-700 truncate">{portalData.candidate.email}</p>
                </div>

                {portalData.candidate.phone && (
                  <div>
                    <p className="text-xs text-muted-foreground mb-1">Phone</p>
                    <p className="font-medium text-slate-700">{portalData.candidate.phone}</p>
                  </div>
                )}

                {portalData.candidate.location && (
                  <div>
                    <p className="text-xs text-muted-foreground mb-1">Location</p>
                    <p className="font-medium text-slate-700">{portalData.candidate.location}</p>
                  </div>
                )}
              </CardContent>
            </Card>

            {/* Professional Details */}
            <Card>
              <CardHeader>
                <CardTitle className="flex items-center gap-2">
                  <Briefcase className="w-5 h-5" />
                  Professional
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-4">
                {portalData.candidate.jobTitle && (
                  <div>
                    <p className="text-xs text-muted-foreground mb-1">Title</p>
                    <p className="font-semibold text-slate-900">{portalData.candidate.jobTitle}</p>
                  </div>
                )}

                {portalData.candidate.experience && (
                  <div>
                    <p className="text-xs text-muted-foreground mb-1">Experience</p>
                    <p className="font-medium text-slate-700">{portalData.candidate.experience}</p>
                  </div>
                )}

                {portalData.candidate.availableIn && (
                  <div>
                    <p className="text-xs text-muted-foreground mb-1">Available In</p>
                    <p className="font-medium text-slate-700">{portalData.candidate.availableIn}</p>
                  </div>
                )}

                {!portalData.candidate.jobTitle && !portalData.candidate.experience && !portalData.candidate.availableIn && (
                  <p className="text-sm text-muted-foreground italic">No professional details added yet</p>
                )}
              </CardContent>
            </Card>

            {/* Online Presence */}
            <Card>
              <CardHeader>
                <CardTitle className="flex items-center gap-2">
                  <Globe className="w-5 h-5" />
                  Online Presence
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-3">
                {portalData.candidate.linkedin && (
                  <a
                    href={portalData.candidate.linkedin}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex items-center gap-3 p-3 bg-blue-50 hover:bg-blue-100 rounded-lg border border-blue-200 transition-colors"
                  >
                    <Linkedin className="w-5 h-5 text-blue-600" />
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-medium text-blue-900">LinkedIn Profile</p>
                      <p className="text-xs text-blue-700 truncate">View profile →</p>
                    </div>
                  </a>
                )}

                {portalData.candidate.portfolioUrl && (
                  <a
                    href={portalData.candidate.portfolioUrl}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex items-center gap-3 p-3 bg-purple-50 hover:bg-purple-100 rounded-lg border border-purple-200 transition-colors"
                  >
                    <Globe className="w-5 h-5 text-purple-600" />
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-medium text-purple-900">Portfolio</p>
                      <p className="text-xs text-purple-700 truncate">View portfolio →</p>
                    </div>
                  </a>
                )}

                {portalData.candidate.resume_url && (
                  <a
                    href={portalData.candidate.resume_url}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="flex items-center gap-3 p-3 bg-amber-50 hover:bg-amber-100 rounded-lg border border-amber-200 transition-colors"
                  >
                    <FileText className="w-5 h-5 text-amber-600" />
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-medium text-amber-900">Resume</p>
                      <p className="text-xs text-amber-700 truncate">View resume →</p>
                    </div>
                  </a>
                )}

                {!portalData.candidate.linkedin && !portalData.candidate.portfolioUrl && !portalData.candidate.resume_url && (
                  <p className="text-sm text-muted-foreground italic">No links added yet</p>
                )}
              </CardContent>
            </Card>

            {/* Personality Vibe */}
            {portalData.candidate.psychAssessment && portalData.candidate.psychAssessment.animal && (
              <Card className="border-2 border-amber-200 bg-gradient-to-br from-amber-50/50 to-orange-50/30">
                <CardHeader>
                  <CardTitle className="flex items-center gap-2">
                    <Brain className="w-5 h-5 text-amber-700" />
                    <span className="text-amber-900">Your Vibe</span>
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  <div className="flex flex-wrap gap-2">
                    {portalData.candidate.psychAssessment.animal && (
                      <Badge variant="secondary" className="bg-white border-2 border-amber-300 text-amber-900 font-semibold px-4 py-2 text-sm">
                        {portalData.candidate.psychAssessment.animal}
                      </Badge>
                    )}
                    {portalData.candidate.psychAssessment.color && (
                      <Badge variant="secondary" className="bg-white border border-amber-200 text-amber-800 px-3 py-1.5 text-xs">
                        {portalData.candidate.psychAssessment.color}
                      </Badge>
                    )}
                    {portalData.candidate.psychAssessment.environment && (
                      <Badge variant="secondary" className="bg-white border border-amber-200 text-amber-800 px-3 py-1.5 text-xs">
                        {portalData.candidate.psychAssessment.environment}
                      </Badge>
                    )}
                    {portalData.candidate.psychAssessment.symbol && (
                      <Badge variant="secondary" className="bg-white border border-amber-200 text-amber-800 px-3 py-1.5 text-xs">
                        {portalData.candidate.psychAssessment.symbol}
                      </Badge>
                    )}
                  </div>
                </CardContent>
              </Card>
            )}
          </div>
